# Kanso 领域词汇表

Kanso 是一个**从零自研**的轻量看板项目管理工具：Go 后端、React 薄前端、SQLite 单机存储、
单二进制部署、内网自用（1-3 人）。领域模型与 UI 设计借鉴 Kaneo（参考仓库
已随开发完成移除），代码不基于它——详见 ADR-0001。
本词汇表记录产品领域语言；实现细节见 `docs/adr/`。

## 访问与身份

**Access Key（访问密钥）**:
服务端持有的唯一共享密钥，前端首次输入后获得全部操作权限。
来源：环境变量 `KANSO_ACCESS_KEY` 优先，未配置则启动时随机生成并打印到控制台。
_Avoid_: 密码、token、API key

**Admin Identity（管理员身份）**:
单一固定身份，所有请求以它执行。任务负责人、评论作者、活动记录全部归属该身份。
_Avoid_: 用户账号、登录名

**Full-access Mode（全权模式）**:
无账号体系、无成员列表、无角色区分——持有访问密钥即拥有全部权限。
_Avoid_: 权限分级、角色管理、邀请

## 核心业务

**Workspace（工作区）**:
顶层容器，包含项目。单用户场景下默认只有一个工作区。
_Avoid_: 团队、组织

**Project（项目）**:
工作区下的项目，含多个列（column）和任务。
_Avoid_: 板块、空间

**Board（看板）/ Column（列）**:
项目的可视化面板及其纵列，任务在列间拖拽移动。
_Avoid_: 泳道、阶段

**Task（任务）**:
最小的可执行工作单元，可被指派（指派对象固定为 Admin Identity）、加标签、评论。
_Avoid_: 事项、工单

**Label（标签）** / **Comment（评论）** / **Activity（活动记录）**:
任务上的分类标记 / 讨论 / 操作轨迹（状态变更、指派等）。
_Avoid_: 分类、备注、日志

**Activity Item（活动条目）**:
单条活动记录的统一渲染单元（前端组件），吃结构化字段
`{projectName, action, createdAt}`，内部持有「在 X 中，你 Y」句子模板与 projectName 高亮。
活动页、仪表盘最近活动共用；任务详情的紧凑句式（「Admin · 动作」）不接入。
_Avoid_: 活动文案、活动渲染器

## 检索与度量（M3 扩展）

**Global Search（全局搜索）**:
FTS5 索引任务标题/描述/评论，命令面板式搜索直达任务。首个 M3 功能。
_Avoid_: 站内查找、过滤器

**Saved View（保存视图）**:
按项目/标签/列维度过滤看板并保存为命名视图；存浏览器本地，非服务端。
_Avoid_: 过滤器、书签、视图服务

**Project Template（项目模板）**:
新建项目时预设初始列名列表，之后列仍可自由增删改名。
_Avoid_: 项目预设、固定列模板

**Task Template（任务模板）**:
预设标题/描述/标签，新建任务时套用，加速重复性任务录入。
_Avoid_: 任务预设、快捷任务

**Cycle Time（周期时间）**:
任务从进入中间列（进行中）到进入末列（完成）的时长，从 activity 的 task.moved 推导，无需录入。任务详情显示单任务耗时，dashboard 显示项目平均。
_Avoid_: 耗时、周转、自动工时

**Time Entry（工时记录）**:
手动按日明细录入任务耗时（新表），dashboard 汇总。与 Cycle Time 并存——周期时间是流程指标，工时是人力投入记录。
_Avoid_: 计时器、自动计时、打卡

**WIP Limit（在制限制）**:
列的并发任务数上限，超限时软警告（列头红色 + 计数），不阻断操作。
_Avoid_: 列限额、容量限制、硬阻止

**Column Position Semantics（列位置语义）**:
状态由列位置定义而非列名：首列=待办、末列=完成、中间列=进行中。重命名列不影响统计与周期时间口径。
_Avoid_: 固定列名、列名匹配

## 新维度（M4 扩展）

**Due Date（截止日期）**:
任务的到期时间字段，看板卡片显示徽章（临期变红），dashboard 展示临期任务。给任务引入时间维度。
_Avoid_: 期限、DEADLINE、到期日（与"截止日期"同义但保持单一）

**Calendar View（日历视图）**:
按截止日期在日历上展示任务的视图，支持拖拽改期。看板之外的第二种任务视图。
_Avoid_: 时间线、甘特图

**Priority（优先级）**:
任务的 Urgent/High/Med/Low 四档字段，卡片角标展示，可排序。替代"紧急"标签 hack。
_Avoid_: 紧急程度、级别、重要度

**Quick Capture（快速捕获）**:
全局快捷键 + 悬浮按钮弹出快速添加框，任意页面一键创建任务。Todoist/Linear 式 GTD 入口。
_Avoid_: 快速添加、收件箱（Inbox 概念 M4 不做）、快捷任务

**Milestone（里程碑）**:
项目内独立实体（名称+日期），任务可关联；dashboard 展示里程碑进度。项目阶段节点。
_Avoid_: 版本、阶段、Sprint（迭代 M4 不做）

**Archive（归档）**:
任务归档后从看板隐藏，可取消归档恢复；不参与 dashboard 统计但可被全局搜索命中。与硬删除/不做回收站并存。
_Avoid_: 回收站、软删除、冷数据

**Swimlane（泳道）**:
看板横向分组，按标签分组（列仍纵向推进）。Jira/Trello 式多维度视图。
_Avoid_: 横列、分区、阶段

## 实现范围

**Slim Scope（精简范围）**:
MVP（M1）已完成：工作区/项目/列/任务/标签/评论/活动/实时（WebSocket）。
M3 扩展：全局搜索（FTS5）、保存视图、项目/任务模板、周期时间、工时（手动录入）、WIP 限制、CSV 导出。
M4 扩展：截止日期、日历视图、优先级、快速捕获、里程碑、任务归档、泳道（按标签）。
后续候选：通知、MCP/AI、公开分享（见 ADR-0003/0007/0008）。
明确不做：计费订阅、第三方集成、附件存储、回收站（保持硬删除+确认弹窗）、自动化规则（列移动即状态表达）、重复任务、任务依赖、子任务/清单、项目归档、收件箱（见 ADR-0007/0008）。
_Avoid_: 完整版、上游原版
