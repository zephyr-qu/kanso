version: "2"
sql:
  - engine: "sqlite"
    schema: "schema.sql"
    queries: "queries"
    gen:
      go:
        package: "gen"
        out: "gen"
        emit_json_tags: true
        json_tags_case_style: camel
        emit_pointers_for_null_types: true
